name: Fetch Hydro Stations Data
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  scrape-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Fetch and Process Data
        run: |
          pip install requests
          python3 -c '
          import requests
          import json
          from datetime import datetime

          # สถานีที่ต้องการ (รหัสสถานีของกรมชลฯ)
          stations = {"C.2": "C.2", "C.13": "C.13", "C.7A": "C.7A", "C.36": "C.36", "C.46": "C.46", "C.47": "C.47"}
          final_data = []
          
          # ดึงข้อมูลจาก API ของ ThaiWater (ข้อมูลชุดเดียวกับกรมชลฯ)
          url = "https://api.thaiwater.net:8443/api/v1/tele/station/latest?group=tele_waterlevel"
          # หมายเหตุ: ในการใช้งานจริงต้องมี API Key หากยังไม่มี ให้ลองใช้การดึงจากหน้าเว็บกรมชลฯ ผ่านตัวช่วย
          # เนื่องจาก API มีการป้องกัน ผมขอแนะนำให้ใช้เครื่องมือ "Apps Script" ดึงผ่าน UrlFetchApp โดยตรงจากหน้าเว็บด้วยการทำ "User-Agent" หลอกระบบว่าเราเป็น Browser
          '
